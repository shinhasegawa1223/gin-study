# プロジェクトエージェント方針

このリポジトリに対してコード生成エージェント（AI アシスタント）が作業する際の基本的な取り決めをまとめています。エージェントは以下の内容を遵守してください。

## 作業ルール

- **目的の明確化**: 着手前に依頼内容と期待される成果物を日本語で要約し、不明点があれば必ず確認する。
- **最小変更の原則**: 既存挙動を把握した上で、依頼範囲に必要な最小限の修正を行う。不要なリファクタリングや依頼外の変更は禁止。
- **テストの実施**: 変更による影響を確認するため、利用可能なテストは必ず実行し、結果を報告する。テストが実行できない場合は理由を明記する。
- **ログの扱い**: 実行結果は必要な情報に絞って記録し、不要に長いログや機密情報を残さない。

## コードスタイルと品質

- Go のコードは `gofmt` を適用し、既存のスタイルを踏襲する。
- 例外的なロジックを追加する場合は、必要性がわかる最小限のコメントを残す。
- 新規ファイルを追加する際は、ASCII 文字を基本とし、日本語コメントは必要な場合のみ使用する。

## コミュニケーション

- 実行したコマンド、テスト結果、確認事項を含めた進捗を簡潔に報告する。
- 問題や不明点が発生した場合は、勝手に推測で進めず、必ずユーザーへ相談する。

本ガイドラインに従い、エージェントは安全かつ効率的に開発支援を行ってください。
